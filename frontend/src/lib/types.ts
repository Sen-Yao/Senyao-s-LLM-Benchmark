export type Leaderboard = { dimensions: string[]; rows: Array<{model_id:number; model:string; provider?:string; overall:number|null; dimensions:Record<string,number|null>; coverage:{current:number; total:number; status:string}}> };
export type TaskChange = {id:number; task_slug:string; change_type:string; old_hash:string; new_hash:string; requires_rerun:boolean; created_at:string};
export type Task = {id:number; slug:string; title:string; category:string; dimension:string; task_type:string; evaluator_type:string; config?:Record<string,unknown>; agent_api_context?:Record<string,unknown>|null; content_hash:string; semantic_hash?:string; evaluator_version?:string; active:boolean; source_path?:string; prompt?:string; description?:string; short_description?:string; evaluator_config?:Record<string,unknown>; covered_models:number; stale_models:number; pending_models:number; total_models:number; latest_change?:TaskChange|null};
export type CoverageModel = {id:number; display_name:string; model_id:string; provider_id:number};
export type TaskCoverage = {task_slug:string; semantic_hash:string; evaluator_version:string; covered_model_ids:number[]; stale_model_ids:number[]; pending_model_ids:number[]; covered_models:number; stale_models:number; pending_models:number; total_models:number; models:{covered:CoverageModel[]; stale:CoverageModel[]; pending:CoverageModel[]}};
export type Provider = {id:number; name:string; api_base:string; api_key_saved:boolean; api_key_fingerprint:string; enabled:boolean; notes:string};
export type ToolProtocol = 'openai_function' | 'anthropic_tool';
export type Model = {id:number; provider_id:number; provider_name?:string; display_name:string; model_id:string; enabled:boolean; tool_protocol:ToolProtocol};
export type Judge = {id:number; provider_id:number; provider_name?:string; name:string; model_id:string; temperature:number; enabled:boolean};
export type RunProgress = {total:number; completed:number; failed:number; running:number; pending:number; cancelled?:number; unknown?:number; accounted?:number; missing?:number; percent:number; current_task:string};
export type Run = {id:number; run_id:string; model_id:number; model_display_name?:string; model_api_id?:string; provider_name?:string; status:string; total_score:number|null; total_latency?:number|null; started_at:string|null; finished_at:string|null; created_at?:string|null; updated_at?:string|null; progress:RunProgress; failure_summary:{count:number; latest_error:string}};
export type RunResult = {task_id:number; task_slug?:string; task_title?:string; dimension?:string; score:number|null; status:string; response:string; judge_reason:string; error:string; latency?:number|null; attempt_count?:number; max_retries?:number; started_at?:string|null; finished_at?:string|null; created_at?:string|null; updated_at?:string|null; freshness?:string; active_status?:string; active_run_id?:string; run_id?:string};
export type ModelCurrentResults = {model_id:number; model_display_name:string; model_api_id:string; provider_name:string; overall:number|null; total_latency:number; coverage:{current:number; stale:number; pending:number; failed:number; running:number; total:number; status:string}; results:RunResult[]};
export type AppSettings = {default_judge_profile_id:number|null; default_judge?:Judge|null};
